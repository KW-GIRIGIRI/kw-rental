import useModal from "../../../hook/useModal"


export default function ReturnModal({modal, setModal, item, handleModifyCartEquip}) {
  const { Modal, open, close } = useModal()
 

  return (
    <Modal className="modify">

      </Modal>
  )
}
